apiVersion: v1
kind: Service
metadata:
  name: {{ include "diabetes-risk.fullname" . }}
  labels:
    {{- include "diabetes-risk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diabetes-risk.selectorLabels" . | nindent 4 }}

